import org.gradle.api.initialization.resolve.RepositoriesMode

//import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}
rootProject.name = "ImplementAuroApp"
include ':app',':auroscholarapp'

//project(':auroscholarapp').projectDir = new File(settingsDir, '../auroscholarapp')

